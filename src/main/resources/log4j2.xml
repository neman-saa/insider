<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>

        <Console name="ConsoleAppender" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d [%t] %-5level %logger{36} - %msg%n%throwable"/>
        </Console>

        <!-- Separate file for TransfersProcessorImpl -->
        <RollingFile name="TransfersProcessorImpl"
                     fileName="logs/transfers.log"
                     filePattern="logs/transfers-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p %c - %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>

    </Appenders>

    <Loggers>

        <Logger name="org.github.insider.alchemy.processors.TransfersProcessorImpl"
                level="info"
                additivity="false">
            <AppenderRef ref="TransfersProcessorImpl"/>
        </Logger>

        <Logger name="com.clickhouse.jdbc"
                level="error"
                additivity="false">
            <AppenderRef ref="ConsoleAppender"/>
        </Logger>

        <Logger name="com.clickhouse.client.api.Client"
                level="error"
                additivity="false">
            <AppenderRef ref="ConsoleAppender"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="ConsoleAppender"/>
        </Root>

    </Loggers>
</Configuration>
